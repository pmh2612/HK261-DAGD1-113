# Copy this file to `.env` and fill in your own values.
# `.env` is git-ignored: never commit real credentials.

# --- Neo4j -------------------------------------------------------------
# Used by the application AND by docker-compose.yml to create the database.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=

# --- External APIs -----------------------------------------------------
# Optional, but gives higher rate limits: https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=
# --- Local LLM ---------------------------------------------------------
# The model is loaded into the Python process with `transformers`. There is no
# inference server and no API call of any kind (see docs/decisions.md DECIDE-7).
# Hugging Face repo id, or a path to downloaded weights. Empty until DECIDE-7.
LLM_MODEL=
# auto | cuda | mps | cpu
LLM_DEVICE=auto

# --- Storage -----------------------------------------------------------
# Where raw/ and processed/ data live. Relative paths resolve from the repo root.
DATA_DIR=data
