"""Data collection from paper sources.

Will contain the Semantic Scholar and arXiv API clients (search, pagination,
rate-limit handling, retries), plus normalization and deduplication of the
records they return into one common paper format.
"""
