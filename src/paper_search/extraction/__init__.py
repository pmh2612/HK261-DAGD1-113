"""Information extraction from paper text.

Will contain spaCy NER over abstracts, LLM prompts that return methods,
datasets and tasks as structured JSON, and entity-name normalization
(e.g. "BERT-base" and "BERT" mapped to one entity).
"""
