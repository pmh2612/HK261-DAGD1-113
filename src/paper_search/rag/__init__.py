"""Retrieval-augmented generation (Phase 2 placeholder).

Will contain the answer-generation prompts that restrict the LLM to the
retrieved context, inline citations mapping back to paper IDs and sections,
and the "no relevant information found" fallback.
"""
